fix: Docker 部署权限适配 PUID/PGID；配置保存链路修复

- 新增 deploy/entrypoint.sh：容器以 root 启动，按 PUID/PGID（默认 1000:1000）
  自动 chown /data 后经 su-exec 降权运行——解决绑定挂载下 /data 不可写导致
  设置页保存配置失败的问题，部署无需手动 chmod（与其他镜像一致）
- Dockerfile：安装 su-exec、改 root 启动 + ENTRYPOINT、ENV PUID/PGID；
  docker-compose 新增 PUID/PGID 环境变量
- server/config.js：save 改原子写（临时文件+rename）+ 返回结果；
  update 返回 { config, saved }；PUT /api/config 保存失败返回 500 明确错误；
  设置页显示保存失败提示
- 修复 5 处 pino 日志占位符（错误详情不再被吞）；启动时数据目录可写性自检
- .gitattributes：shell 脚本强制 LF；.dockerignore 放开 entrypoint.sh
- README 补充权限说明与 PUID/PGID
